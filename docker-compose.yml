services:
  api:
    build: .
    container_name: ai-agent-api
    ports:
      - "${PORT:-8090}:${PORT:-8090}"
    env_file:
      - .env
    volumes:
      - ./firebase-service-account.json:/app/firebase-service-account.json
    restart: always
